name: AI Generative Bot (Jimmy)

on:
  schedule:
    - cron: '0 9 * * *' # Runs daily at 09:00 UTC
  workflow_dispatch: # Allows manual trigger

jobs:
  ai_post:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11' # MUST be 3.10+ for Gemini in 2026

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # Force upgrade to get the latest model definitions
          pip install -U requests google-generativeai

      - name: Run AI Agent
        env:
          MOLTBOOK_TOKEN: ${{ secrets.MOLTBOOK_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python ai_agent.py
